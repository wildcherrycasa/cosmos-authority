[
  { "$discharged": "✅ OBLIGATION DISCHARGED 2026-09-06 on merged master (1400b01): T1 is now CAUGHT by evidence B3d/B3f and auth B3h. C13 is load-bearing, not decorative. The history below is kept because it is the useful part — the assertion existed and passed for hours while being incapable of failing, and only merging the branch that introduced the field made it real." },
  { "$comment": "⚠ HISTORY — THIS SPEC SURVIVED BEFORE #1 MERGED, BY CONSTRUCTION, AND THAT WAS RECORDED RATHER THAN HIDDEN. Raised in review 2026-09-06: 'six weeks from now someone reasons we added auth to /approvals, so approvals are authenticated, and flips the flag' — reintroducing the exact lie PR #1 exists to prevent, via the PR that appears to fix it. Measured on the review/write-auth line (branched from master): T1 SURVIVED. Reason: `approver_authenticated` does not exist in core/receipt.js until PR #1 merges, and buildPayload constructs an explicit object, so a value passed up from api/approvals.js is silently dropped and nothing observable changes. The auth suite's C13 therefore passes VACUOUSLY here — a control that cannot match is indistinguishable from one that is working (the same trap that hid the vendored gate's failure earlier the same day). OBLIGATION: whichever of PR #1 / PR #2 / PR #3 merges LAST must re-run this spec; it must then be CAUGHT by auth C13. If it still survives after #1 has merged, C13 is decorative and needs rewriting, not re-running." },
  { "file": "api/approvals.js",
    "name": "T1 the write token is taken as proof the APPROVER was authenticated (the six-week refactor)",
    "from": "        checks, idempotency_key: 'approval:' + id, decided_at: now,",
    "to": "        checks, idempotency_key: 'approval:' + id, decided_at: now, approver_authenticated: true," }
]
